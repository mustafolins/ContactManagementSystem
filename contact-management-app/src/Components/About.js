import React, { Component } from 'react'

export default class About extends Component {
    render() {
        return (
            <div>
                This webapp is written in react and interfaces with a very simple contact management api.
            </div>
        )
    }
}
